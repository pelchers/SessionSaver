# Repo-scoped skills

Create a subfolder per skill:

```
.codex/skills/<skill-name>/SKILL.md
```

`SKILL.md` should use YAML front matter with at least `name` and `description`.
Keep skills small and task-focused. Use optional `scripts/`, `references/`, and `assets/` folders only when they add value.
