# frontend-design-subagent

Generates one isolated style/pass frontend concept in `.docs/planning/concepts/<style>/pass-<n>` by writing all HTML/CSS/JS from scratch (no template stamping). Background images are optional. Animation libraries are style-dependent, not forced. Writes validation outputs to that pass's `validation/` subfolder.
