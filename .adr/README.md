# ADR Workspace (Project-Specific)

This ADR area was reset after importing `.codex` from another project.

Use these folders for this repository only:
- `current/` active session phase files
- `history/` completed phase reviews
- `orchestration/` orchestration plans and phase documents
- `agent_ingest/` imported agent notes
